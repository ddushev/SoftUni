<main class="details">
    <!-- Details page -->
    <section id="details">
        <div id="details-wrapper">
            <p id="details-title">Name: {{item.name}}</p>
            <div id="img-wrapper">
                <img src="{{item.imageUrl}}" />
            </div>
            <div id="info-wrapper">
                <table>
                    <tr>
                        <td>
                            <p>
                                Years: <span id="details-years">{{item.years}}</span>
                            </p>
                        </td>
                        <td>
                            <p>Kind: <span id="details-kind">{{item.kind}}</span></p>
                        </td>
                    </tr>
                    <tr>
                        <td>
                            <p>Need of: <span id="details-need">{{item.need}}</span></p>
                        </td>
                        <td>
                            <p>Location: <span id="details-location">{{item.location}}</span></p>
                        </td>
                    </tr>
                </table>
                <p>Description: <span id="details-description">{{item.description}}</span></p>
            </div>

            <!--Edit and Delete are only for creator-->
            {{#if hasUser}}
            <div id="action-buttons">
                {{#if item.isOwner}}
                <a href="/item/{{item._id}}/edit" id="edit-btn">Edit</a>
                <a href="/item/{{item._id}}/delete" id="delete-btn">Delete</a>
                {{else}}
                {{#unless item.isInteracted}}
                <a href="/item/{{item._id}}/interact" id="donation-btn">Donation</a>
                {{else}}
                <p id="already-donated">You already donated!</p>
                {{/unless}}
                {{/if}}
            </div>
            {{/if}}
        </div>
    </section>
</main>